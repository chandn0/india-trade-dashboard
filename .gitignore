# dependencies
node_modules

# next.js
.next
out

# production
build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# local claude state (keep launch.json, ignore per-user allowlist)
.claude/settings.local.json

# scratch
tmp

# Data zip bundle — its contents (yearly_raw.json, yearly_summary.csv, sources.md)
# are already tracked individually; the zip is redundant.
data/*.zip
